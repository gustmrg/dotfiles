# Shared Agent Skills

Versioned skills shared by multiple agent tools.

`bootstrap.sh` can link `agents/skills` to `~/.agents/skills`.

Use this directory for skills that should be discoverable outside a single tool-specific config directory.
